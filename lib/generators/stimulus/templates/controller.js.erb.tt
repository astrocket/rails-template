// <div data-controller="<%%= controller_pattern %>" data-<%%= controller_pattern %>-hello-value="Hello from">
//
// </div>
import { Controller } from "stimulus";
import api from 'utils/api';
import helpers from 'utils/helpers';

export default class extends Controller {
  static targets = [ "" ];
  static values = {
    hello: String
  }

  connect() {
    const hello = this.hasHelloValue ? this.helloValue : "Something Wrong";
    console.log(`${hello} <%%= stimulus_path %>`);
  }
}